class MenuManager {
  #info;
  #category;
  #coaches;

  constructor(info, coaches) {
    this.#info = info;
    this.#coaches = coaches;
  }

  shuffle(category) {}
}

export default MenuManager;
